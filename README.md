# PPMAndSubs

Beta version of the PPM and Subs web application.

## Setup

Create virtual environment:

python -m venv venv

Activate it and install dependencies:

pip install -r requirements.txt

## Run

python app.py